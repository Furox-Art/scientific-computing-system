name: PR Automation

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to run automation for'
        required: true
        type: string

# One run per PR. `synchronize` fires on every push to a PR branch, so without
# this a burst of commits queues a labeler run per push.
concurrency:
  group: pr-automation-${{ github.event.pull_request.number || inputs.pr_number }}
  cancel-in-progress: true

# Deny by default; each job re-grants only what it needs. `review-checklist`
# previously declared nothing and therefore inherited the repository default,
# which was read-write at the time this was written.
permissions: {}

jobs:
  labeler:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@v7
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"

  review-checklist:
    runs-on: ubuntu-latest
    # Posts a comment on the PR via the issues API, which for a pull request
    # is gated by `pull-requests: write`.
    permissions:
      contents: read
      pull-requests: write
    # Runs on label 'needs-review' or manual dispatch
    if: |
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'needs-review')) ||
      github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            let prNumber = context.issue ? context.issue.number : null;

            if (context.eventName === 'workflow_dispatch') {
              prNumber = parseInt('${{ inputs.pr_number }}');
            }

            if (!prNumber) {
              console.log('No PR number provided');
              return;
            }

            // Avoid duplicate comments
            const { data: comments } = await github.rest.issues.listComments({
              owner, repo, issue_number: prNumber
            });

            const hasChecklist = comments.some(c =>
              c.body.includes('PR Review Checklist') && c.user.type === 'Bot'
            );

            if (hasChecklist) {
              console.log('Review checklist comment already exists');
              return;
            }

            const checklist = `## PR Review Checklist

            Maintainer quick checklist for this PR:

            - [ ] Tests added or updated where needed
            - [ ] Documentation updated (README, docstrings, examples)
            - [ ] No unnecessary dependencies added
            - [ ] Code follows project style (run ruff locally)
            - [ ] CHANGELOG updated if user-facing

            Feel free to ping the maintainer for review.`;

            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: prNumber,
              body: checklist
            });

            console.log(`Posted review checklist for PR #${prNumber}`);
